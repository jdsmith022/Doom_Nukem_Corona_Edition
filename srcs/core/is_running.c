#include "../../includes/doom.h"
#include "../../includes/audio.h"
#include "../../includes/gameplay.h"
#include "../../includes/menu.h"

// void	doom_is_not_running(t_doom *doom)
// {
// 	// while (doom->is_running == FALSE)
	// {
		// set_up_menu(doom);
	// 	SDL_UpdateWindowSurface(doom->window);
	// 	ft_bzero(doom->surface->pixels, sizeof(doom->surface->pixels));
	// 	printf("help\n");
	// }
	// if (doom->menu->start == TRUE)
	// 	doom->is_running = TRUE;
// }

// void	doom_is_running(t_doom *doom)
// {
// 	if (doom->game_editor == FALSE)
// 	{
// 		// sprite_reset(doom);
// 		doom_render(doom);
// 		audio(doom->audio, &doom->own_event);
// 		doom_gui(doom);
// 		groceries(doom);
// 		sliding_door(doom, -1); //move to its own file
// 		add_infection(doom); //move to a seprate file
// 	}
// 	else
// 		open_game_editor(doom);
// }
