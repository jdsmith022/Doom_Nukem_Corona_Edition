#include "../../includes/doom.h"
#include "../../includes/font.h"

void		font_timer(t_doom *doom, double dt)
{
	// if (doom->lib.font_lib.instruction == TRUE && dt > 20 && dt < )
	// 	draw_font(doom, doom->lib.font_lib.instruction_font,\
	// 		doom->lib.font_lib.instruction_font_len);
	// else
	// 	doom->lib.font_lib.instruction = FALSE;
	
}